{
  "name": "price-pulse-backend",
  "version": "1.0.0",
  "description": "Express backend for Price-Pulse — uses SQLite to store products, stores, categories and users, serves them as JSON.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "init-db": "node init-db.js"
  },
  "dependencies": {
    "axios": "^1.15.0",
    "cheerio": "^1.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sql.js": "^1.11.0"
  }
}
